<% if current_user_can_edit?(photo)
    button_destroy =
      link_to 'x', event_photo_path(photo.event, photo),
        class: 'border-0 bg-white d-inline-block',
        method: :delete,
        data: {confirm: t('.destroy_confirm'), remote: true }
end %>

<% image_title = "Добавил #{render partial: 'users/user_mini', object: photo.user} #{button_destroy}" %>

<%= link_to image_tag(photo.photo.thumb.url, width: '100', height: '100'), 
  photo.photo.url, title: image_title, class: 'image-link m-1',
  data: { toggle: 'lightbox', gallery: 'event-photos', lightbox: "set"} %>
